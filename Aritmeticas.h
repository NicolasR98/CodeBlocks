#include <stdio.h>
///bloques de comentarios
/** \brief (funcion)
 *
 * \param (parametro)
 * \param
 * \return
 *
 */

/** \brief pide un numero y lo valida
           en un rango especifico
 *
 * \param char[] texto que se va a mostrar
 * \param int    un entero que representa un minimo valor dentro del rango
 * \param int    el maximo valor dentro del rango
 * \return int   el numero ingresado
 *
 */
int pedirEnetero(char[], int, int);
